<%= form_for(@payment_type) do |f| %>
  <% if @payment_type.errors.any? %>
    <div id="error_explanation">
      <h4><%= pluralize(@payment_type.errors.count, "error") %> prohibited this payment_type from being saved:</h4>

      <ul>
      <% @payment_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description, id: 'message_area' %>
  </div>
  <div class="actions">
    <%= f.submit class: 'btn btn-primary' %>
  </div>
<% end %>
